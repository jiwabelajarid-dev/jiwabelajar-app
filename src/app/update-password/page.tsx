// app/update-password/page.tsx
import UpdatePassword from "@/components/Auth/UpdatePassword"; // Sesuaikan path ke komponen baru Anda

export default function UpdatePasswordPage() {
  // Komponen ini tidak perlu melakukan apa-apa selain merender komponen client
  return <UpdatePassword />;
}